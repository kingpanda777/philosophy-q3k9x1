# 作業用バックアップ。ローカルには残すが GitHub には上げない。
_old_backups/

# 作業中の .bak も GitHub には上げない（_old_backups へ移す前の状態）
*.bak-*

# 典拠として取得した PDF などの置き場。Pages で配信されるので上げない
tools/_sources/

# 点検用の一時ファイル
detailonly.json

# 確認用・作業用のスクリプトが書き出すファイルの置き場（2026年9月23日）。Pages で配信されるので上げない
tools/_work/

# 道具が書き込みに使う一時ファイル（<名前>.tmp-<pid>）。途中で強制終了したときだけ残る
*.tmp-*

# Claude Code のこのプロジェクト用・自分の PC 用の設定（hooks など。2026年9月26日）。中身は CLAUDE.md「hooks で書き換えの決まりを止める」に写してある
.claude/settings.local.json

# Windows / エディタが勝手に作るファイル
Thumbs.db
desktop.ini
.DS_Store
